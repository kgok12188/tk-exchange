---
description: Kafka 按币对 topic 的分区约定，避免考虑多分区
globs: 
  - "**/*.java"
  - "**/doc/**"
  - "**/openspec/**"
---

# Kafka 按币对 Topic：单分区约定

- **`match_result_(币对)`** 与 **`order_req_(币对)`** 每个 topic **仅一个分区**（partition 0）。
- 设计上按币对严格顺序处理，不依赖多分区；实现时只使用 **partition 0**，无需考虑多分区、rebalance 或按分区聚合。
- 命名示例：`match_result_BTC-USDT`、`order_req_BTC-USDT`（币对格式与项目一致，如连字符或下划线）。
