<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>


    <groupId>tk</groupId>
    <artifactId>tk-exchange</artifactId>
    <version>1.0</version>
    <packaging>pom</packaging>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.11</version>
    </parent>

    <properties>
        <!-- Spring Boot uses java.version for compiler + test; must match SBE tool / Aeron. -->
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <mybatis-plus-boot-stater.version>3.5.3.1</mybatis-plus-boot-stater.version>
        <mysql-connector-java.version>8.0.33</mysql-connector-java.version>
        <lombok.version>1.18.30</lombok.version>
        <redisson-spring-boot-starter.version>3.24.3</redisson-spring-boot-starter.version>
        <kafka.version>4.2.0</kafka.version>
        <fastjson2.version>2.0.60</fastjson2.version>
        <!-- Align with io.aeron:aeron-client (Aeron 1.50.0 → agrona 2.4.0). Do not use non-existent patch versions. -->
        <agrona.version>2.4.0</agrona.version>
        <sbe.version>1.34.1</sbe.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.alibaba.fastjson2</groupId>
                <artifactId>fastjson2</artifactId>
                <version>${fastjson2.version}</version>
            </dependency>
            <dependency>
                <groupId>com.lmax</groupId>
                <artifactId>disruptor</artifactId>
                <version>3.4.4</version>
            </dependency>
            <dependency>
                <groupId>org.roaringbitmap</groupId>
                <artifactId>RoaringBitmap</artifactId>
                <version>1.3.0</version>
            </dependency>
            <dependency>
                <groupId>org.agrona</groupId>
                <artifactId>agrona</artifactId>
                <version>${agrona.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <modules>
        <module>trading-protocol</module>
        <module>tk-common</module>
        <module>tk-trading-server</module>
        <module>tk-match-engine</module>
        <module>tk-open-api</module>
        <module>tk-job-service</module>
        <module>tk-trigger-server</module>
        <module>tk-admin-api</module>
        <module>trading-flush</module>
    </modules>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>enforce-java</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[21,)</version>
                                    <message>
                                        JDK 21+ is required (SBE sbe-tool, Spring Boot 3.5, project source level 21).
                                        Set JAVA_HOME to a JDK 21 installation.
                                    </message>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>